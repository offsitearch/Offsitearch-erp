"""Projects module — projects, phases, team membership, timeline.

Owns: the ``projects``, ``project_phases`` and ``project_team`` tables
(``Project``, ``ProjectPhase``, ``ProjectTeam`` models) and their business logic.
Status: type templates extracted (Wave 2); full module extraction scheduled next.
"""
