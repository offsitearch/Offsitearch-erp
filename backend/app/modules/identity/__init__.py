"""Identity module — users, refresh tokens, authentication.

Owns: the ``users`` and ``refresh_tokens`` tables (``User``,
``RefreshToken`` models), authentication/session logic, and the
auth + user-administration routes.
"""
