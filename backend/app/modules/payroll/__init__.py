"""Payroll module — payroll runs, entries, payslip PDFs, salary proration.

Owns: the ``payroll_runs`` and ``payroll_entries`` tables (``PayrollRun``,
``PayrollEntry`` models) and their business logic.
"""
