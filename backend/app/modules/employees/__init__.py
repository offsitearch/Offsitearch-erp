"""Employees module — employee directory, profiles, documents, salary, org chart.

Owns: the ``employee_documents`` table (``EmployeeDocument`` model) and
employee-management business logic operating on the shared ``User`` aggregate.
"""
